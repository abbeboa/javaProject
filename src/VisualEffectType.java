/**
 * VisualEffect enum class. Variable VisualEffectType set in AbstractVisualEffectType and
 */

public enum VisualEffectType {
    /**
     * Explosion when ship is destroyed
     */
    EXPLOSION
}
